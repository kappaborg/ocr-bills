# Frontend production env. Paste these into Vercel → Project → Settings → Environment Variables.
# (Do NOT commit a real .env.production file — it would shadow Vercel's UI-defined vars.)

# Public — embedded into the JS bundle at build time. Use HTTPS in production.
NEXT_PUBLIC_API_BASE_URL=https://api.your-domain.com
